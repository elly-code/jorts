i18n.gettext(app_id,
    args: [
        '--directory=' + meson.project_source_root(),
        '--from-code=UTF-8',
        '-cTRANSLATORS',
    ],
    preset: 'glib'
)

if appstream
    subdir('extra')
endif
